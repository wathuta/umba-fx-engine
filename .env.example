POSTGRES_DB=fx_takehome
POSTGRES_USER=fx_user
POSTGRES_PASSWORD=fx_password
POSTGRES_HOST=localhost
POSTGRES_PORT=55432

# Optional: set DATABASE_URL only when you want to override the URL built from
# POSTGRES_* values, for example in CI or with a managed database provider.
# DATABASE_URL=postgresql+psycopg://fx_user:fx_password@localhost:55432/fx_takehome

RATE_PROVIDER_URL=https://fxapi.app/api/usd.json
